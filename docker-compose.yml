services:
  linkharbor:
    build: .
    container_name: linkharbor
    ports:
      - "3000:3333"
    volumes:
      - ./storage:/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - DATA_PATH=/data
