services:
  linkharbor:
    build: .
    container_name: linkharbor
    ports:
      - "127.0.0.1:3333:3000"
    volumes:
      - ./storage:/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - DATA_PATH=/data
